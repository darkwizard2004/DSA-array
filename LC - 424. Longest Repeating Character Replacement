this is done on 24 september 2026 

problem - 
You are given a string s and an integer k. You can choose any character of the string and change it to any other uppercase English character. You can perform this operation at most k times.

Return the length of the longest substring containing the same letter you can get after performing the above operations.

Example 1:
Input: s = "ABAB", k = 2
Output: 4
Explanation: Replace the two 'A's with two 'B's or vice versa.

Example 2:
Input: s = "AABABBA", k = 1
Output: 4
Explanation: Replace the one 'A' in the middle with 'B' and form "AABBBBA".
The substring "BBBB" has the longest repeating letters, which is 4.
There may exists other ways to achieve this answer too.
 

Constraints:
1 <= s.length <= 105
s consists of only uppercase English letters.
0 <= k <= s.length

solution - 
class Solution {
public:
    int characterReplacement(string s, int k) {
        //TC = O(n) and SC = O(1);

        unordered_map<char,int> freq;

        int left = 0;
        int maxAns = 0;
        int maxFreq = 0;

        for(int right = 0; right < s.size(); right++){
            freq[s[right]]++;
            maxFreq = max(maxFreq,freq[s[right]]);

            while((right-left+1) - maxFreq > k){

                freq[s[left]]--;

                if(freq[s[left]] == 0){
                    freq.erase(s[left]);
                }

                left++;

            }

            maxAns = max(maxAns, right-left+1);
        }

        // since there are only 26 english alphabets we can use array instead of hash or unordered map, but time and space complexity is still : O(n) and O(1) respectively.

        // use static array:

        // int freq[26] = {0};

        // int left = 0;
        // int maxAns = 0;
        // int maxFreq = 0;

        // for(int right = 0; right < s.size(); right++){
        //     freq[s[right] - 'A']++;
        //     maxFreq = max(maxFreq, freq[s[right] - 'A']);

        //     while((right-left+1) - maxFreq > k){
        //         freq[s[left] - 'A']--;
        //         left++;
        //     }
        //     maxAns = max(maxAns,right - left + 1);
        // }

        return maxAns;
    }
};
